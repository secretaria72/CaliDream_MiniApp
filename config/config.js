// Configuration pour modules activables (signalement, feedback, etc)
const config = {
  enableReport: false,
  enableFeedback: false,
  avis_button: true // ✅ Affiche le bouton 💬 si true, sinon il disparaît
};
